apiVersion: v1
kind: Service
metadata:
  name: manufacturing-api
  namespace: industrial-robotics
spec:
  selector:
    app: manufacturing-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
