apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "manufacturing-api.fullname" . }}-config
  labels:
    {{- include "manufacturing-api.labels" . | nindent 4 }}
data:
  SERVICE_NAME: {{ .Values.appConfig.SERVICE_NAME | quote }}
  ENVIRONMENT: {{ .Values.appConfig.ENVIRONMENT | quote }}